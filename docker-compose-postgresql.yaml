version: "3.7"
services:
  database:
    image: postgres:13.3-alpine
    environment:
      POSTGRES_DB: SpectatorWebApp
      POSTGRES_USER: TEST
      POSTGRES_PASSWORD: TEST
    ports:
      - "5432:5432"
